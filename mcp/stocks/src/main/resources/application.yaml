server:
  port: 8012

spring:
  application:
    name: stocks
  ai:
    mcp:
      server:
        enabled: true
        type: ASYNC
        annotation-scanner:
          enabled: true  # Enable/disable annotation scanning

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/mcpdb
    username: sa
    password: root
